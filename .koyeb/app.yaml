name: billing-system-mongodb

services:
  - name: web
    build:
      type: docker
      dockerfile: Dockerfile
    
    ports:
      - port: 4555
        protocol: http
    
    routes:
      - path: /
        port: 4555
    
    env:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "4555"
    
    scaling:
      min: 1
      max: 1
    
    regions:
      - fra
    
    instance_type: nano
    
    health_checks:
      - type: http
        port: 4555
        path: /health
        interval: 30
        timeout: 10
        grace_period: 60
